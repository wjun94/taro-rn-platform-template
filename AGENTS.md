# 前端 App 开发规范

本文件适用于项目根目录及其所有子目录。默认使用中文沟通、编写功能注释和说明变更；代码标识符沿用项目的英文命名习惯。

## 项目约定

- 使用现有的 Taro、React、React Native、TypeScript 和 Less 技术栈，依赖版本以 `package.json` 与 `pnpm-lock.yaml` 为准。
- 统一使用 pnpm，保留现有锁文件，不混用 npm / yarn 生成新的锁文件。
- 开始修改前阅读相关页面、组件和配置，沿用既有目录结构、命名、缩进与代码风格。
- 只修改与当前需求相关的内容，不顺带重构、升级依赖或批量格式化无关文件。
- 优先复用现有组件、工具和依赖；仅在需求确实需要时引入新依赖。

## 目录与职责

- `src/app.ts`：应用入口与全局生命周期。
- `src/app.config.ts`：页面注册与全局窗口配置。
- `src/app.less`：全局样式；页面专属样式放在对应页面目录。
- `src/pages/`：页面、页面配置与页面样式，新增页面时同步注册路由。
- 随功能需要按需创建 `src/components/`、`src/hooks/`、`src/services/`、`src/utils/` 和 `src/types/`，分别存放共享组件、共享逻辑、接口访问、纯工具函数和共享类型；不要提前创建空目录。
- `config/`：Taro 及环境配置。
- `android/`、`ios/`：原生工程，仅在需求涉及原生能力或平台配置时修改。
- 不手动修改 `node_modules/`、`ios/Pods/`、构建产物或自动生成文件。

## 代码与功能注释

- React 组件优先采用函数组件和 Hooks，组件名使用 PascalCase，函数与变量使用 camelCase，自定义 Hook 使用 `use` 前缀。
- 为组件 Props、接口请求与响应、关键业务数据定义明确的 TypeScript 类型；避免无理由使用 `any`、非空断言或忽略类型错误。
- 页面负责组织界面与交互，复杂业务逻辑按需拆分为 Hook 或服务函数；避免过度抽象和超大组件。
- **新增或修改的页面、组件、Hook、业务函数和关键业务分支必须有必要的中文功能注释。** 修改功能时同步更新注释，保证描述与实现一致。
- 功能注释说明用途、输入输出约束、业务规则或特殊处理原因；简单赋值、显而易见的 JSX 和基础语法不逐行注释。
- 对外复用的函数、组件和 Hook 优先使用 JSDoc；复杂流程、平台差异、权限处理和兼容逻辑在对应代码附近注释。
- 临时方案需说明原因和适用范围，不留下无说明的魔法数字、占位实现或过期注释。

功能注释示例：

```ts
/** 将商品金额从分转换为元，供页面统一展示两位小数。 */
export function formatPrice(amountInCents: number): string {
  return (amountInCents / 100).toFixed(2)
}
```

## 页面与交互

- 页面补齐加载中、空数据、请求失败及正常展示状态，失败时提供合理提示或重试入口。
- 提交、保存等异步操作防止重复触发，结束后恢复交互状态；表单包含必要校验与明确错误反馈。
- 列表使用稳定且唯一的 key；长列表、图片和频繁更新的区域按实际需要优化，避免无依据地添加缓存或记忆化。
- 异步请求、订阅、定时器和事件监听应处理清理与竞态，避免页面卸载后继续更新状态。
- 样式优先放入对应 Less 文件，复用通用颜色、间距和字号，减少重复样式与无必要的全局覆盖。
- 适配移动端安全区、键盘遮挡、滚动、小屏幕与文字溢出，交互元素提供清晰反馈和必要的无障碍标识。

## 跨端与数据处理

- 优先使用项目已有的 Taro 组件与 API；使用 React Native 或其他平台专有能力时明确适用平台并隔离实现。
- 不在共享代码中直接依赖浏览器 DOM、`window` 或 `document`；新增样式和 API 时关注目标平台兼容性。
- 涉及相机、相册、定位等系统能力时处理权限申请、拒绝、取消和不可用状态，必要时同步原生权限配置。
- 接口访问统一收口到服务层，处理异常与响应数据，不将网络请求细节散落在展示组件中。
- 不将密钥、令牌或用户隐私写入源码、注释和日志；环境差异沿用现有环境配置。

## 测试与验证限制

- **不新增测试文件**，包括 `*.test.*`、`*.spec.*`、`__tests__/`、快照及端到端测试文件。
- 不为当前任务引入测试框架、测试依赖或测试脚本；默认不执行自动化测试。已有测试文件不因本约定而删除。
- **不运行打包或构建命令来验证代码**，包括 `build:*`、`taro build`、React Native bundle、Gradle 构建、Xcode 构建或归档等。
- `dev:*` 脚本可能间接调用构建，`pnpm android` / `pnpm ios` 也会触发原生编译；不要将这些命令作为默认验证步骤，也不要自动启动开发服务或模拟器。
- 默认通过阅读修改后的代码检查导入、类型使用、调用关系、路由配置和边界逻辑。需要额外静态检查时，仅使用已有工具进行有针对性且不生成产物的检查，不为验证安装依赖。
- 用户后续明确要求测试、运行或打包时，以该次明确要求为准。

## 交付说明

- 完成后简要说明修改内容、涉及文件与实际检查方式。
- 明确说明未执行测试和打包验证，不将代码阅读或静态检查描述为运行验证通过。
- 如有需要用户实际体验的交互或平台差异，提供简短的手动检查要点。
